# Android build outputs
**/build/
.gradle/
.idea/
*.iml

# Local SDK paths
local.properties

# Captures
captures/

# Keystore (test-only keystore committed intentionally)
# release-key.jks  -- NOT ignored, committed for reproducible signing

# Note: dist/ is intentionally NOT ignored. The latest signed release
# APK is checked in at dist/wechat2docx-release.apk so users can download
# it directly from the repo. Keep this dir small (single APK).
